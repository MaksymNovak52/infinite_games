import {
  HeaderContainer,
  HeroContainer,
  LeaderBoardContainer,
  PredictionsContainer,
} from "@/components";

export default function Home() {
  return (
    <main>
      <HeaderContainer />
      <HeroContainer />
      <LeaderBoardContainer />
      <PredictionsContainer />
    </main>
  );
}
